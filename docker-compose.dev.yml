services:
  meta-blog:
    container_name: meta-blog-co
    build:
      context: .
      args:
        - NODE_ENV=development
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
    volumes:
      - .:/app/src
    command: npm run dev
